#include "main.h"
/**
 * add - show the sum of two integers
 * Description: 'prints the sum of two variables'
 * @i: the input i is an integer
 * @j: the input j is an integer
 * Return: the sum of two integers
 */
int add(int i, int j)
{
	return (i + j);
}
